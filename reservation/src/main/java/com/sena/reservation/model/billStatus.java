package com.sena.reservation.model;

public enum billStatus {
    PENDING,
    COMPLETED,
    FAILED,
    REFUNDED,
    DISPUTED,
    CANCELLED,
    PROCESSING,
    ON_HOLD,
    DECLINED,
    EXPIRED;
}
